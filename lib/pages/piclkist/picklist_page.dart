import 'package:flutter/material.dart';

class PicklistPage extends StatelessWidget {
  const PicklistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Picklist Page'));
  }
}